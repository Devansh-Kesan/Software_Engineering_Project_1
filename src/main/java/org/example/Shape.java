package org.example;

/**
 * Shape interface shows abstraction.
 * It only tells that shape can be drawn,
 * but does not provide any information about how it will be drawn.
 */
public interface Shape {

    /**
     * method to draw shape.
     */
    void draw();
}